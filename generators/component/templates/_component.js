import * as React from 'react';
import './styles.scss'

export const <%= component %>  = () => (
  <div className="<%= className %>">
    <%= component %> component
  </div>
);

export default <%= component %>;
